# frozen_string_literal: true

RSpec.describe UberTask do
  it "has a version number" do
    expect(UberTask::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
